# 🏗️ Pizza POS - System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         WINDOWS KIOSK                                │
│                      (15-21" Touchscreen)                            │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                   Chrome Kiosk Mode                         │    │
│  │                        or                                   │    │
│  │                   Electron App                              │    │
│  │                                                             │    │
│  │  ┌───────────────────────────────────────────────────┐    │    │
│  │  │         React Frontend (Vite + TypeScript)        │    │    │
│  │  │                                                    │    │    │
│  │  │  Components:                                       │    │    │
│  │  │  • TouchButton (60-80px)                          │    │    │
│  │  │  • PizzaPreview                                   │    │    │
│  │  │  • OrderCard                                      │    │    │
│  │  │  • KitchenCard                                    │    │    │
│  │  │                                                    │    │    │
│  │  │  Pages:                                            │    │    │
│  │  │  • HomePage → NewOrder → PizzaBuilder            │    │    │
│  │  │  • SidesAndDrinks → Checkout → Success           │    │    │
│  │  │  • KitchenView (separate screen)                 │    │    │
│  │  │                                                    │    │    │
│  │  │  State:                                            │    │    │
│  │  │  • Zustand (cartStore, pizzaBuilderStore)        │    │    │
│  │  │  • React Context (MenuContext)                    │    │    │
│  │  │                                                    │    │    │
│  │  │  Styling:                                          │    │    │
│  │  │  • Tailwind CSS (touch-optimized)                │    │    │
│  │  │  • Custom pizza-themed colors                     │    │    │
│  │  └─────────────────┬──────────────────────────────┘    │    │
│  │                     │                                     │    │
│  └─────────────────────┼─────────────────────────────────────┘    │
│                        │                                           │
└────────────────────────┼───────────────────────────────────────────┘
                         │
                         │ HTTP/HTTPS + WebSocket
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    Node.js Backend Server                            │
│                   (Express + Socket.io)                              │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                    Express REST API                          │    │
│  │                                                              │    │
│  │  GET  /api/menu          → MenuService.getMenuData()       │    │
│  │  GET  /api/menu/toppings → MenuService.getToppings()       │    │
│  │  POST /api/orders        → OrderService.createOrder()      │    │
│  │  GET  /api/orders        → OrderService.getAllOrders()     │    │
│  │  GET  /api/orders/:id    → OrderService.getOrder()         │    │
│  │  PATCH /api/orders/:id/status                              │    │
│  │  POST /api/orders/:id/pay                                  │    │
│  │  GET  /api/orders/pending                                  │    │
│  └────────────────────────┬───────────────────────────────────┘    │
│                            │                                        │
│  ┌────────────────────────┴───────────────────────────────────┐    │
│  │              Socket.io WebSocket Server                     │    │
│  │                                                              │    │
│  │  Events:                                                     │    │
│  │  • join:kitchen      → Join kitchen room                   │    │
│  │  • order:created     → New order notification              │    │
│  │  • order:status      → Status update                       │    │
│  │  • order:paid        → Payment confirmation                │    │
│  │  • order:new         → Broadcast to kitchen               │    │
│  │  • order:updated     → Broadcast update                    │    │
│  │  • order:completed   → Remove from kitchen                │    │
│  └────────────────────────┬───────────────────────────────────┘    │
│                            │                                        │
│                            ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │                  Business Logic Layer                     │      │
│  │                                                            │      │
│  │  MenuService                  OrderService                │      │
│  │  • getMenuData()              • createOrder()             │      │
│  │  • getToppings()              • getOrder()                │      │
│  │                                • getAllOrders()            │      │
│  │                                • getPendingOrders()        │      │
│  │                                • updateOrderStatus()       │      │
│  │                                • markAsPaid()              │      │
│  │                                • deleteOrder()             │      │
│  └────────────────────────┬───────────────────────────────────┘    │
│                            │                                        │
│                            ▼                                        │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │                  SQLite Database                          │      │
│  │                  (better-sqlite3)                         │      │
│  │                                                            │      │
│  │  Tables:                                                   │      │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │      │
│  │  │   sizes    │  │   crusts   │  │  toppings  │         │      │
│  │  │────────────│  │────────────│  │────────────│         │      │
│  │  │ id         │  │ id         │  │ id         │         │      │
│  │  │ name       │  │ type       │  │ name       │         │      │
│  │  │ base_price │  │ modifier   │  │ price      │         │      │
│  │  └────────────┘  └────────────┘  │ category   │         │      │
│  │                                   └────────────┘         │      │
│  │                                                            │      │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │      │
│  │  │menu_items  │  │   orders   │  │order_items │         │      │
│  │  │────────────│  │────────────│  │────────────│         │      │
│  │  │ id         │  │ id         │  │ id         │         │      │
│  │  │ name       │  │ total      │  │ order_id   │         │      │
│  │  │ category   │  │ status     │  │ type       │         │      │
│  │  │ price      │  │ payment    │  │ name       │         │      │
│  │  └────────────┘  │ notes      │  │ price      │         │      │
│  │                   │ created_at │  │ quantity   │         │      │
│  │                   └────────────┘  │ custom_pizza│        │      │
│  │                                   └────────────┘         │      │
│  │                                                            │      │
│  │  File: server/data/pos.db                                │      │
│  └──────────────────────────────────────────────────────────┘      │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### Creating an Order

```
┌──────────┐
│  User    │
│ (Touch)  │
└────┬─────┘
     │ 1. Build Pizza
     ▼
┌──────────────────┐
│ PizzaBuilderPage │
│  (React)         │
└────┬─────────────┘
     │ 2. Add to cartStore
     ▼
┌──────────────────┐
│   cartStore      │
│   (Zustand)      │
└────┬─────────────┘
     │ 3. Navigate to Checkout
     ▼
┌──────────────────┐
│  CheckoutPage    │
│                  │
└────┬─────────────┘
     │ 4. POST /api/orders
     ▼
┌──────────────────┐
│  Express API     │
│                  │
└────┬─────────────┘
     │ 5. OrderService.createOrder()
     ▼
┌──────────────────┐
│ SQLite Database  │
│  (pos.db)        │
└────┬─────────────┘
     │ 6. Return order object
     ▼
┌──────────────────┐
│  Express API     │
│                  │
└────┬─────────────┘
     │ 7. POST /api/orders/:id/pay
     ▼
┌──────────────────┐
│ OrderService     │
│  .markAsPaid()   │
└────┬─────────────┘
     │ 8. Update database
     ▼
┌──────────────────┐
│ Socket.io Emit   │
│ order:created    │
└────┬─────────────┘
     │ 9. Broadcast to kitchen
     ▼
┌──────────────────┐
│  KitchenView     │
│   (listening)    │
└──────────────────┘
```

### Real-Time Kitchen Updates

```
┌──────────────────┐         ┌──────────────────┐
│   POS Terminal   │         │  Kitchen Display │
│                  │         │                  │
│  CheckoutPage    │         │  KitchenViewPage │
└────────┬─────────┘         └────────┬─────────┘
         │                            │
         │ 1. Payment Complete        │ 3. Listen for events
         │                            │
         ▼                            ▼
┌─────────────────────────────────────────────────┐
│          Socket.io Server (Backend)             │
│                                                  │
│  socket.on('order:paid', (order) => {          │
│    io.to('kitchen').emit('order:new', order);  │
│  });                                            │
│                                                  │
│  socket.on('order:status', ({ id, status }) => {│
│    io.emit('order:updated', { id, status });   │
│  });                                            │
└─────────────────────────────────────────────────┘
         │                            │
         │ 2. Emit order:paid         │ 4. Receive order:new
         │                            │
         ▼                            ▼
┌──────────────────┐         ┌──────────────────┐
│  Success Screen  │         │  Display Order   │
│                  │         │  Card (Real-time)│
└──────────────────┘         └──────────────────┘
                                      │
                                      │ 5. Mark Complete
                                      ▼
                             ┌──────────────────┐
                             │ PATCH /api/orders│
                             │  /:id/status     │
                             └──────────────────┘
```

### Menu Data Loading

```
┌──────────────────┐
│   Application    │
│     Starts       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  MenuProvider    │
│   (Context)      │
└────────┬─────────┘
         │ useEffect(() => fetchMenu())
         ▼
┌──────────────────┐
│ GET /api/menu    │
│                  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  MenuService     │
│  .getMenuData()  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  SQLite Query    │
│  • sizes         │
│  • crusts        │
│  • toppings      │
│  • menu_items    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   Return JSON    │
│   MenuData       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Store in State  │
│  (React Context) │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  All Components  │
│  Access via      │
│  useMenu() hook  │
└──────────────────┘
```

---

## Component Architecture

### Frontend Component Hierarchy

```
App (Router)
│
├── MenuProvider (Context)
│   │
│   ├── HomePage
│   │   └── TouchButton (×3)
│   │
│   ├── NewOrderPage
│   │   └── TouchButton (×2)
│   │
│   ├── PizzaBuilderPage
│   │   ├── TouchButton (×many)
│   │   ├── PizzaPreview
│   │   └── Uses: pizzaBuilderStore, MenuContext
│   │
│   ├── SidesAndDrinksPage
│   │   ├── TouchButton (grid)
│   │   └── Uses: cartStore, MenuContext
│   │
│   ├── CheckoutPage
│   │   ├── OrderItemCard (×n)
│   │   ├── TouchButton (payment)
│   │   └── Uses: cartStore
│   │
│   └── KitchenViewPage
│       ├── KitchenOrderCard (×n)
│       ├── TouchButton (refresh)
│       └── Uses: useSocket hook
```

### Backend Service Architecture

```
Express App
│
├── Middleware
│   ├── CORS
│   ├── express.json()
│   └── Error Handler
│
├── Routes
│   ├── /api/menu → menuRoutes
│   │   └── MenuService
│   │       └── Database
│   │
│   └── /api/orders → orderRoutes
│       └── OrderService
│           └── Database
│
└── Socket.io
    ├── connection event
    ├── join:kitchen event
    ├── order:created event
    ├── order:status event
    └── order:paid event
```

---

## State Management Architecture

### Client-Side State

```
┌─────────────────────────────────────────────┐
│          Application State                   │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Global State (React Context)      │    │
│  │                                     │    │
│  │  MenuContext                        │    │
│  │  • menuData: MenuData | null       │    │
│  │  • loading: boolean                 │    │
│  │  • error: string | null            │    │
│  │  • refetch: () => void             │    │
│  └────────────────────────────────────┘    │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Cart State (Zustand)              │    │
│  │                                     │    │
│  │  cartStore                          │    │
│  │  • items: OrderItem[]              │    │
│  │  • addItem()                        │    │
│  │  • removeItem()                     │    │
│  │  • updateQuantity()                 │    │
│  │  • clearCart()                      │    │
│  │  • getTotal()                       │    │
│  └────────────────────────────────────┘    │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Pizza Builder State (Zustand)     │    │
│  │                                     │    │
│  │  pizzaBuilderStore                  │    │
│  │  • selectedSize: Size | null       │    │
│  │  • selectedCrust: Crust | null     │    │
│  │  • selectedToppings: string[]      │    │
│  │  • setSize()                        │    │
│  │  • setCrust()                       │    │
│  │  • toggleTopping()                  │    │
│  │  • reset()                          │    │
│  │  • isComplete()                     │    │
│  └────────────────────────────────────┘    │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Socket State (Custom Hook)        │    │
│  │                                     │    │
│  │  useSocket()                        │    │
│  │  • socket: Socket | null           │    │
│  │  • connected: boolean               │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

### Server-Side State

```
┌─────────────────────────────────────────────┐
│           Server State                       │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Persistent State (SQLite)          │    │
│  │                                     │    │
│  │  • sizes (menu data)                │    │
│  │  • crusts (menu data)               │    │
│  │  • toppings (menu data)             │    │
│  │  • menu_items (sides/drinks)        │    │
│  │  • orders (order records)           │    │
│  │  • order_items (order details)      │    │
│  └────────────────────────────────────┘    │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  Runtime State (Memory)             │    │
│  │                                     │    │
│  │  • Socket.io connections            │    │
│  │  • Active rooms (kitchen)           │    │
│  │  • Express middleware               │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

---

## Deployment Architecture

### Development Environment

```
┌────────────────┐         ┌────────────────┐
│  Developer PC  │         │  Browser       │
│                │         │                │
│  Terminal 1:   │───────► │  localhost:5173│
│  npm run dev   │  Vite   │  (Frontend)    │
│  (client)      │         │                │
└────────────────┘         └────────────────┘

┌────────────────┐         ┌────────────────┐
│  Developer PC  │         │  API Testing   │
│                │         │                │
│  Terminal 2:   │───────► │  localhost:3001│
│  npm run dev   │  tsx    │  (Backend API) │
│  (server)      │         │                │
└────────────────┘         └────────────────┘
```

### Production (Chrome Kiosk)

```
┌──────────────────────────────────────┐
│      Windows Kiosk Machine           │
│                                       │
│  ┌──────────────────────────────┐   │
│  │  Process 1: Backend Server    │   │
│  │  node server/dist/index.js    │   │
│  │  Port: 3001                   │   │
│  └──────────────────┬────────────┘   │
│                     │                 │
│  ┌──────────────────▼────────────┐   │
│  │  Process 2: Chrome Kiosk      │   │
│  │  chrome.exe --kiosk           │   │
│  │  http://localhost:5173        │   │
│  │  (Serves built client)        │   │
│  └───────────────────────────────┘   │
│                                       │
│  Auto-start via:                      │
│  • startup folder                     │
│  • task scheduler                     │
│  • registry                           │
└──────────────────────────────────────┘
```

### Production (Electron)

```
┌──────────────────────────────────────┐
│      Windows Kiosk Machine           │
│                                       │
│  ┌───────────────────────────────┐  │
│  │  Electron Application          │  │
│  │  (Single Executable)           │  │
│  │                                │  │
│  │  ┌──────────────────────────┐ │  │
│  │  │ Main Process (Node.js)    │ │  │
│  │  │ • Backend Server          │ │  │
│  │  │ • Window Management       │ │  │
│  │  │ • Kiosk Mode              │ │  │
│  │  └────────┬──────────────────┘ │  │
│  │           │                    │  │
│  │  ┌────────▼──────────────────┐ │  │
│  │  │ Renderer Process          │ │  │
│  │  │ • React Frontend          │ │  │
│  │  │ • UI Components           │ │  │
│  │  └───────────────────────────┘ │  │
│  └───────────────────────────────┘  │
│                                       │
│  Auto-start via Task Scheduler        │
└──────────────────────────────────────┘
```

---

## Network Communication

### HTTP REST API

```
Client                          Server
  │                               │
  │  GET /api/menu               │
  ├──────────────────────────────►│
  │                               │ MenuService.getMenuData()
  │                               │ ↓
  │  200 OK + MenuData           │ Database Query
  │◄─────────────────────────────┤
  │                               │
  │  POST /api/orders            │
  │  Body: { items: [...] }      │
  ├──────────────────────────────►│
  │                               │ OrderService.createOrder()
  │                               │ ↓
  │  201 Created + Order         │ Insert into DB
  │◄─────────────────────────────┤
  │                               │
```

### WebSocket Real-Time

```
POS Client          Server          Kitchen Client
    │                 │                    │
    │ Connect          │                   │
    ├────────────────►│                   │
    │ Handshake       │                   │
    │◄────────────────┤                   │
    │                 │                   │
    │                 │   Connect         │
    │                 │◄──────────────────┤
    │                 │   join:kitchen    │
    │                 │◄──────────────────┤
    │                 │ (Join room)       │
    │                 │                   │
    │ order:paid      │                   │
    ├────────────────►│                   │
    │                 │ order:new         │
    │                 ├──────────────────►│
    │                 │ (Broadcast)       │
    │                 │                   │
    │                 │  order:status     │
    │                 │◄──────────────────┤
    │ order:updated   │                   │
    │◄────────────────┤ order:updated     │
    │                 ├──────────────────►│
    │ (Both receive)  │ (Broadcast all)   │
```

---

*Complete system architecture for Pizza Shop POS - All layers documented*
