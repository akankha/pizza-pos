{
  "buildCommand": "cd client && npm ci && npm run build && cd ../server && npm ci --include=dev && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.js"
    }
  ]
}
