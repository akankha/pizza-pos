{
  "name": "pizza-pos",
  "version": "1.0.0",
  "description": "Pizza Shop POS System for Windows Touchscreen Kiosks",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm install --include=dev && npm run build",
    "build:server": "cd server && npm install --include=dev && npm run build",
    "vercel-build": "npm run build:server",
    "start": "cd server && node dist/index.js",
    "start:production": "NODE_ENV=production cd server && node dist/index.js",
    "preview": "npm run build && npm run start",
    "electron:dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"wait-on http://localhost:5173 && electron .\"",
    "electron:build": "electron-builder",
    "clean": "rm -rf client/dist server/dist client/node_modules server/node_modules node_modules",
    "deploy:vercel": "git push origin master",
    "deploy:electron": "npm run electron:build",
    "deploy:hostinger": "npm run build:client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^39.2.4",
    "electron-builder": "^24.9.1",
    "wait-on": "^7.2.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
